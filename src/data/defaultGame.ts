// These should NOT be used - they're here only for type compatibility
// The app should fail gracefully when Farcaster data isn't available
export const defaultPairs: string[] = [];

// These should NOT be used - they're here only for type compatibility
// The app should fail gracefully when Farcaster data isn't available
export const defaultRevealImages: string[] = [];

export const defaultMessage = "Will you accept my lub?";